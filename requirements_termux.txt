# Agent Zero - Termux Compatible Requirements
# ============================================
# Modified requirements for ARM/Android (Termux) compatibility
# Some heavy packages are replaced with lighter alternatives or API-based services

# Core Flask/Web
a2wsgi>=1.10.8
flask[async]>=3.0.3
flask-basicauth>=0.2.0
aiohttp>=3.9.0
httpx>=0.25.0
requests>=2.31.0

# Async utilities
ansio>=0.0.1
nest-asyncio>=1.6.0

# LLM Integration (using LiteLLM for provider abstraction)
litellm>=1.60.0  # Updated for 2026
tiktoken>=0.8.0
openai>=1.60.0
google-generativeai>=0.8.0  # Google AI Studio support (Gemini)

# LangChain (core only, minimal dependencies)
langchain-core>=0.4.0
langchain-community>=0.4.0

# Search
duckduckgo-search>=6.1.12

# Git operations
GitPython>=3.1.43

# Shell/Process utilities
paramiko>=3.5.0
psutil>=7.0.0
inputimeout>=1.0.4

# Expression evaluation (for tools)
simpleeval>=1.0.3

# Document parsing (lightweight alternatives)
pypdf>=6.0.0
pymupdf>=1.25.3
beautifulsoup4>=4.12.3
lxml>=4.9.0
lxml_html_clean>=0.3.1
html2text>=2024.2.26
markdownify>=1.1.0
markdown>=3.7

# Data handling
pydantic>=2.11.7
python-dotenv>=1.1.0
pytz>=2024.2
webcolors>=24.6.0
pathspec>=0.12.1

# Scheduling
crontab>=1.0.1

# Email (optional)
imapclient>=3.0.1
# exchangelib>=5.4.3  # Heavy, skip for Termux

# Vector storage (using ChromaDB instead of FAISS for better ARM compatibility)
chromadb>=0.4.0
# faiss-cpu  # Replaced with chromadb

# Embeddings (use API-based instead of local sentence-transformers)
# sentence-transformers>=3.0.1  # Too heavy for Termux, use API embeddings

# Audio (disabled by default on Termux)
# openai-whisper>=20240930  # Use API-based STT
# kokoro>=0.9.2  # TTS disabled
# soundfile>=0.13.1

# Browser automation (disabled on Termux - no display)
# browser-use>=0.5.11
# playwright>=1.52.0

# MCP (Model Context Protocol) - optional
# mcp>=1.13.1
# fastmcp>=2.3.4
# fasta2a>=0.5.0

# OCR (optional, needs tesseract)
# pytesseract>=0.3.13
# pdf2image>=1.17.0

# Termux-specific additions
aiosqlite>=0.19.0  # For async SQLite operations
